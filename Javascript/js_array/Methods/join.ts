/**
 * Join all array elements into a string (like toString)
 * Can specify the separator
 * Syntax: array.join("separator")
 */
const joinArray = ["Nguyen", "Dinh", "Trong", "PTIT"];
let arrayToString = joinArray.join(" ");
console.log(arrayToString);
//  document.getElementById("join").innerHTML = arrayToString; // Nguyen Dinh Trong PTIT