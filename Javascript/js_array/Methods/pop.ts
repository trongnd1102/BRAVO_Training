/**
 * Remove the latest element from array
 * Syntax: array.pop()
 */
const popArray = ["Nguyen", "Dinh", "Trong", "PTIT"]
popArray.pop();
console.log(popArray);
//  document.getElementById("pop").innerHTML = popArray; // Nguyen,Dinh,Trong

/**
 * pop() method returns the value that was poped out
 */
console.log(popArray.pop())
//  document.getElementById("pop1").innerHTML = popArray.pop(); // Trong